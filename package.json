{
  "name": "draftlab-analisador-de-picks",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve-app.mjs",
    "data:build": "node scripts/build-static-data.mjs",
    "data:profiles": "node scripts/build-champion-profiles.mjs",
    "test": "node --check app/app.js && node --check app/engine.js && node --check app/storage.js && node tests/app-engine.test.mjs"
  }
}
